import React from "react";
import { Building2 } from "lucide-react";
import PlaceholderPage from "../../src/shared/PlaceholderPage";

export default function Societies() {
  return (
    <PlaceholderPage
      icon={Building2}
      title="Societies"
      description="Full registry, onboarding, and governance detail for every member society will live here."
    />
  );
}
