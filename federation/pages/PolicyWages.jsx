import React from "react";
import { ScrollText } from "lucide-react";
import PlaceholderPage from "../../src/shared/PlaceholderPage";

export default function PolicyWages() {
  return (
    <PlaceholderPage
      icon={ScrollText}
      title="Policy & Wages"
      description="Wage floors, policy directives, and compliance rules pushed down to member societies will live here."
    />
  );
}
